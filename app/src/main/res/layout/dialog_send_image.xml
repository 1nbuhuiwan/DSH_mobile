<?xml version="1.0" encoding="utf-8"?>
<!-- 发送图片对话框：圆角卡片 + 圆角输入框 + 次级/主按钮，与全局 DeepSeek 风格一致 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_dialog"
    android:elevation="10dp"
    android:orientation="vertical"
    android:padding="22dp">

    <!-- 标题 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="14dp"
        android:text="@string/image_send_title"
        android:textColor="@color/ds_ink"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- 描述输入框 -->
    <EditText
        android:id="@+id/captionInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_edit"
        android:gravity="top|start"
        android:hint="@string/image_caption_hint"
        android:inputType="textCapSentences|textMultiLine"
        android:lineSpacingExtra="4dp"
        android:maxLines="4"
        android:minLines="2"
        android:padding="14dp"
        android:textColor="@color/ds_ink"
        android:textColorHint="@color/ds_muted"
        android:textSize="15sp" />

    <!-- 按钮区 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="end"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/btnCancel"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:layout_marginEnd="10dp"
            android:background="@drawable/bg_secondary_button"
            android:gravity="center"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:text="@string/image_cancel"
            android:textColor="@color/ds_muted"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/btnSend"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:background="@drawable/bg_brand_button"
            android:gravity="center"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:text="@string/image_send"
            android:textColor="@color/ds_brand_ink"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>
</LinearLayout>
